import resetCSS from './reset.css';
import { hamBurger, renderTaskDOM, newProject } from './modules/interface';
import { addTask } from './modules/task';

// TTD: Logic to remove task from Inbox's and other projects' tasks array, 
// Logic to sort tasks by date, logic to load the task arrays to the DOM
// based on each project
// save everything to local storage